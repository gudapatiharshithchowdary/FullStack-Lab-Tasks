CREATE DATABASE login_system;
USE login_system;

CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL
);

-- Example user (password hashed with PHP's password_hash)
INSERT INTO users (username, password) VALUES ('testuser', '$2y$10$exampleHashedPasswordHere');
